<section id="skills">
    <h5>
        <mark>Skills</mark>
    </h5>
    @if (_isLoaded)
    {
        @foreach (var (name, level) in _skills)
        {
            <div class="row skill">
                <div class="col-8">@name</div>
                <div class="col-4">@level</div>
            </div>
        }
    }
</section>